{
  "name": "sora-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev:sora": "turbo run dev --scope sora",
    "dev:absensi": "turbo run dev --scope absensi",
    "dev:sora-desktop": "turbo run dev --scope sora-desktop",
    "dev:absensi-desktop": "turbo run dev --scope absensi-desktop",
    "build:web": "turbo run build --filter sora absensi",
    "build:desktop": "turbo run build --filter *-desktop"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
