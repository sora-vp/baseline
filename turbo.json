{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "^db:generate"],
      "outputs": [".next/**", "dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "typecheck": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "build-win": {
      "dependsOn": ["build"],
      "outputs": ["dist/**", "out/**"]
    },
    "build-linux": {
      "dependsOn": ["build"],
      "outputs": ["dist/**", "out/**"]
    },
    "db:generate": {
      "outputs": ["generated-prisma/**"]
    }
  }
}
